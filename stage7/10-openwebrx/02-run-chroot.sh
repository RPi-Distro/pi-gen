#!/usr/bin/env bash
set -euo pipefail
# install from here, so we do not install suggested packages too.
# we need the recommended only
#echo DEBIAN_FRONTEND=noninteractive apt install -y --install-recommends openwebrx

